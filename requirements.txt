fastapi==0.115.6
uvicorn[standard]==0.34.0
aiohttp==3.11.11
databricks-sdk==0.67.0
pydantic==2.10.4
python-multipart==0.0.20
httpx==0.28.1
openai==2.14.0
asyncpg==0.30.0
# NOTE: "Ask Everything" now queries a Databricks Agent Bricks Multi-Agent Supervisor
# serving endpoint over REST (see server/routes/supervisor.py). The old in-app langgraph
# + databricks-langchain + mcp stack was removed — it caused MCP/langchain version-skew
# 503s on redeploys, and the managed supervisor handles routing natively.
